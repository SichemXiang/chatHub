<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chatHub.mapper.GroupMapper">


    <insert id="createGroup">
        insert into groups (group_id, group_name, created_at, creator)
        values (#{groupId}, #{groupName}, #{createdAt}, #{creator});
    </insert>
    
    
</mapper>